export type friendlyUser = {
    userId: string,
    firstName: string,
    lastName: string,
}

export default interface FriendlyUser{
    userId: string;
    firstName: string;
    lastName: string;
}